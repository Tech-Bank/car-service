info:
  name: Modyfikacja zgłoszenia
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/maintenance/maintenance-orders/{{order_id}}/change"
  body:
    type: json
    data: |-
      {
        "description": "Wymiana oleju po 1000 km"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
