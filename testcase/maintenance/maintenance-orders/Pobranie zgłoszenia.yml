info:
  name: Pobranie zgłoszenia
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/maintenance/maintenance-orders/{{order_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
