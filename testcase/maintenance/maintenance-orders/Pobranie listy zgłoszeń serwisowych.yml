info:
  name: Pobranie listy zgłoszeń serwisowych
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/maintenance/maintenance-orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
