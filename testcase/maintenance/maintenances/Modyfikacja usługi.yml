info:
  name: Modyfikacja usługi
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/maintenance/maintenances/{{maintenance_id}}/change"
  body:
    type: json
    data: |-
      {
        "description": "Wymiana oleju - ekspresowa"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
