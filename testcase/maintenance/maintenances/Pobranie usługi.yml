info:
  name: Pobranie usługi
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/maintenance/maintenances/{{maintenance_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
