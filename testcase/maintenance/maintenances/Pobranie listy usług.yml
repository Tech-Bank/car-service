info:
  name: Pobranie listy usług
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/maintenance/maintenances"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
