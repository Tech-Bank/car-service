info:
  name: Rejestracja usługi (do zgłoszenia)
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/maintenance/maintenances"
  body:
    type: json
    data: |-
      {
        "maintenanceOrderId": 1,
        "description": "Okresowa wymiana oleju wg zgłoszenia"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
