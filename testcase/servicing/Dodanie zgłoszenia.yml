info:
  name: Dodanie zgłoszenia
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/servicing/requests"
  body:
    type: json
    data: |-
      {
        "vehicleId": 1,
        "description": "Wymiana oleju po 500 km"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
