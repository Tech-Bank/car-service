info:
  name: Pobranie listy zgłoszeń
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/maintenance/maintenance-orders"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
